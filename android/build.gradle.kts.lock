/**
 * 🧩 ANDROID BUILD CONFIGURATION — LOCKED MATRIX (Flutter 3.35.7)
 * ------------------------------------------------------------------
 * This project is aligned to Flutter 3.35.7 (Dart 3.9.2, DevTools 2.48)
 * and uses Gradle / AGP / Kotlin versions that are officially supported
 * by this Flutter SDK release.
 *
 * 🔒 Version Lock Matrix:
 *   • Gradle Wrapper:              8.13
 *   • Android Gradle Plugin (AGP): 8.7.1
 *   • Kotlin Plugin:               2.1.0
 *   • JDK:                         21 LTS
 *   • Google Services Plugin:      4.4.4
 *   • Firebase Crashlytics Plugin: 2.8.1
 *   • Firebase BoM:                34.5.0
 *
 * ⚠️ DO NOT CHANGE THESE VERSIONS unless:
 *   - You’ve upgraded Flutter to a newer stable release
 *   - You’ve confirmed compatibility via the official Flutter Gradle docs:
 *     https://docs.flutter.dev/development/tools/android-studio
 *
 * 💡 After version updates, always run:
 *   flutter clean
 *   flutter pub get
 *   ./gradlew clean
 *   ./gradlew :app:dependencies
 *
 * ------------------------------------------------------------------
 * Last verified: November 2025
 */


plugins {
    id("com.android.application") apply false
    id("org.jetbrains.kotlin.android") apply false
    id("com.google.gms.google-services") version "4.4.4" apply false
    id("com.google.firebase.crashlytics") version "2.8.1" apply false
    id("dev.flutter.flutter-gradle-plugin") apply false
}

buildscript {
    dependencies {
        classpath("org.jetbrains.kotlin:kotlin-gradle-plugin:2.1.0")
    }
}


allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

val newBuildDir: Directory =
    rootProject.layout.buildDirectory
        .dir("../../build")
        .get()
rootProject.layout.buildDirectory.value(newBuildDir)

subprojects {
    val newSubprojectBuildDir: Directory = newBuildDir.dir(project.name)
    project.layout.buildDirectory.value(newSubprojectBuildDir)
}
subprojects {
    project.evaluationDependsOn(":app")
}

tasks.register<Delete>("clean") {
    delete(rootProject.layout.buildDirectory)
}
